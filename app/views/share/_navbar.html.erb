<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand">Task_Web</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li>
        <%= link_to t('tasks.home_page'), root_path(order: params[:order], locale: params[:locale]), class: "nav-link" %>
      </li>

      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <%= t('tasks.language') %>
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <%= link_to "中文版", { order: params[:order], locale: "zh-TW" }, class: "dropdown-item" %>
          <%= link_to "English", { order: params[:order], locale: "en" }, class: "dropdown-item" %>
        </div>
      </li>

        <div class="user">
        <% if current_user %>
          <%= link_to(t('admin.user_management'), admin_users_path, class: "nav-link") if current_user.admin? %>
          <%= link_to current_user.nickname, edit_users_path, class: "nav-link" %>
          <%= link_to t('users.sign_out'), sign_out_users_path, method: 'delete', class: "nav-link" %>
        <% else %>
          <%= link_to t('users.sign_up'), sign_up_users_path, class: "nav-link" %>  
          <%= link_to t('users.sign_in'), sign_in_users_path, class: "nav-link" %>  
        <% end %>
        </div>
    </ul>
  </div>
</nav>
